# Reel Harness configuration (pydantic-settings reads .env in the CWD).
# Copy to .env and fill in real values. NEVER commit .env -- it is gitignored.
# Every value below is a placeholder, not a real credential.

# --- Core ---
DATABASE_URL=sqlite:///./reel_harness.db
JOBS_DIR=./jobs
APP_API_KEY=replace-with-a-long-random-local-api-key
LOG_LEVEL=INFO

# --- Worker lease policy ---
# Heartbeat must stay well below the timeout (<= 1/3) so long renders are
# never reclaimed as stale.
LEASE_TIMEOUT_SECONDS=300
LEASE_HEARTBEAT_SECONDS=60

# --- LLM provider ---
# "fake" (default, no network) or "openai-compatible" (any /chat/completions
# endpoint -- the vendor is chosen purely by LLM_BASE_URL + LLM_MODEL).
LLM_PROVIDER=fake
LLM_BASE_URL=
LLM_MODEL=
# Read from the environment/.env only; registered for log/DB redaction at
# startup and never written to the DB, manifests, or logs.
LLM_API_KEY=
LLM_CONNECT_TIMEOUT_SECONDS=10.0
LLM_READ_TIMEOUT_SECONDS=60.0
LLM_MAX_RETRIES=3
LLM_RETRY_BACKOFF_SECONDS=2.0
LLM_TEMPERATURE=0.7
LLM_MAX_OUTPUT_TOKENS=1200

# --- Media toolchain (optional overrides; see docs/ARCHITECTURE.md) ---
# REEL_HARNESS_FFMPEG_PATH=
# REEL_HARNESS_FFPROBE_PATH=
