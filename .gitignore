.venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
jobs/
*.db
.env
.tools/
fable_projects/
# Local scratch: screenshots and smoke output are inspected by hand and
# regenerated on demand, never reviewed as source.
shots/
capture_ids.txt
smoke_output/

# Credential backups made when repairing a malformed .env -- these hold
# real keys and must never be committed.
.env.bak.*

# Output from fable-reference-smoke and ad-hoc runs: generated media, and
# in the smoke case images that cost money. Kept locally, never in git
# (same rule as fable_projects/).
smoke-out/
story.txt
